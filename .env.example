# ============================================================
# FAQ Chatbot — Environment Variables
# ============================================================
# Copy this file to .env and fill in your values.
# DO NOT commit .env to version control.

# ----- Required -----
GOOGLE_API_KEY=                     # Your Google AI Studio API key (https://aistudio.google.com/apikey)

# ----- Optional (defaults shown) -----
MODEL_NAME=gemini-2.5-flash         # Gemini model for answer generation
SIMILARITY_THRESHOLD=0.75           # Cosine similarity cutoff (0.0 – 1.0)
PORT=4000                           # Backend port

# ----- For PostgreSQL (if migrating from SQLite, see ARCHITECTURE.md §9.2) -----
# DATABASE_URL=postgresql://user:password@localhost:5432/faqbot

# ----- For Email Notifications (see ARCHITECTURE.md §9.4) -----
# SMTP_HOST=smtp.office365.com
# SMTP_PORT=587
# SMTP_USER=noreply@yourcompany.com
# SMTP_PASS=
# ADMIN_EMAIL=team@yourcompany.com
